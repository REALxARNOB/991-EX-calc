## 2024-05-22 - Missing Charset Breaks Math Symbols
**Learning:** Mathematical symbols like `√` and `π` require explicit `<meta charset="UTF-8">` to render correctly in all browsers, otherwise they appear as mojibake.
**Action:** Always verify meta charset in HTML files for scientific/math applications.
