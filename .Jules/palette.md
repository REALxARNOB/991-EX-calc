
## 2024-05-18 - Display Accessibility
**Learning:** Using `readonly` instead of `disabled` on calculator displays allows the element to receive focus and the text to be selectable by screen readers, improving accessibility.
**Action:** Always prefer `readonly` for read-only displays where the content still needs to be accessible to screen readers and keyboard users.
