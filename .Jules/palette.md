## 2024-05-24 - Result Input Accessibility
**Learning:** Using `readonly` over `disabled` for formula/result displays enables text selection and better screen reader compatibility.
**Action:** Prioritize `readonly` with explicit visual blending for result fields instead of using `disabled`.
