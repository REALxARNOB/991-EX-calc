## 2026-02-23 - Calculator Display Accessibility
**Learning:** Using `disabled` attribute on calculator result inputs prevents screen reader users from reading the result and navigating to it.
**Action:** Always use `readonly` attribute combined with `aria-label` for read-only data displays that should be focusable/readable.
