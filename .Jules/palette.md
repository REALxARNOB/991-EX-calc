## 2024-05-24 - Result Input Accessibility
**Learning:** Using `disabled` on result inputs in calculators prevents screen readers from accessing the content and breaks keyboard focus.
**Action:** Use `readonly` instead to allow focus and text selection while preventing manual editing, and apply custom focus-visible styling.
