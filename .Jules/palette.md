## 2026-02-20 - Calculator Display Accessibility
**Learning:** `disabled` inputs are inaccessible for reading/selection and often have unchangeable, poor contrast styles.
**Action:** Use `readonly` with `aria-label` for display-only fields to allow focus, text selection, and custom styling while maintaining semantics.
