## 2024-05-20 - Accessible Read-Only Inputs
**Learning:** Using `disabled` on calculator displays prevents screen readers from accessing the content and prevents users from selecting/copying the result.
**Action:** Use `readonly` with `aria-label` for display inputs, styled with `transparent` background and `focus-visible` outlines to ensure accessibility without breaking the visual design.
