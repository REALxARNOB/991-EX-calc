## 2024-05-24 - Result Input Accessibility
**Learning:** Disabled inputs in calculators prevent screen readers from announcing results and stop users from focusing or copying calculated values.
**Action:** Always use `readonly` with appropriate `aria-label` and focus styles for calculator display inputs instead of `disabled`.
